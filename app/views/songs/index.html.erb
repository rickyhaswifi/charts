<h1>Click a Song To View Details</h1>
<h1>All of the songs for <%= @chart.name %></h1>
<%= link_to 'Create a new song', new_chart_song_path(@chart) %>
<ul>
  <% @songs.each do |song| %>
    <li><%= link_to song.name, [@chart, song] %></li>
  <% end %>
</ul>